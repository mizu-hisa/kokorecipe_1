<h1>編集画面</h1>

<%= form_with model: @recipe, url: recipe_path(@recipe.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :detail %>

  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <%= f.submit '保存' %>

<% end %>